[project]
name = "investment-research-platform"
version = "0.1.0"
description = "投研协作平台 - 基于 OpenClaw 的智能投研协作系统"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
